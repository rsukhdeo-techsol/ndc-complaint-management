rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to attachments folder (Phase 1: open for dev)
    match /attachments/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
